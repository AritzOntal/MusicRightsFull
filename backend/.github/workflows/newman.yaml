name: Integration tests with Newman

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  newman-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
      - run: mvn package -DskipTests
      - run: docker build -t musicrights-api .
      - run: docker compose up -d
        env:
          # Enchufamos ls variables en el sistema
          MUSICRIGHTS_DATABASE_PASSWORD: root_password
          MARIADB_USER: musicrights_user
          MARIADB_DATABASE: musicrights
          MARIADB_ROOT_PASSWORD: root_root
          PROFILE: prod

      - run: sleep 45
      - run: npm install -g newman
      - run: newman run "MusicRights API.postman_collection.json" -e Local.postman_environment.json